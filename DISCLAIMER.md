# Disclaimer

sudocheck is a defensive audit tool for systems you own or are explicitly authorized to test.

The tool prints exploit commands to explain risk, but it does not execute those commands and does not attempt
automatic exploitation. You are responsible for complying with laws, contracts and internal policies.

Use `--hide-exploits` or `--defensive` when sharing reports with audiences that should only see remediation
guidance.

